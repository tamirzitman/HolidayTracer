# Google Sheet that holds all the data.
# Copy the id out of the sheet's URL: docs.google.com/spreadsheets/d/<THIS>/edit
SHEET_ID=

# Service account credentials. Create a service account in Google Cloud,
# enable the Google Sheets API, download a JSON key, then share the sheet
# with the service account's address as an Editor.
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Any long random string. Signs the cookie that remembers a phone number.
SESSION_SECRET=

# Leave SHEET_ID empty to run against a local .dev-sheet.json file instead,
# which is handy before the real sheet exists.

# Set on a deployment pointed at a scratch sheet. Puts a standing "סביבת ניסיון"
# strip across the top, switches off the sign-in gate so any number gets in
# (trying the app out as other people is what the playground is for), and is
# what `npm run reset` checks before emptying a sheet. Never set this on the
# deployment holding the real record.
PLAYGROUND=

# Milliseconds the server holds its in-memory copy of the sheet before
# re-reading it. Defaults to 20000. Lower it when running the smoke suite
# against a local server (e.g. SHEET_TTL_MS=100) so the two checks that wait
# out a stale cache take under a second instead of ~20 each; never set this
# in production.
SHEET_TTL_MS=
